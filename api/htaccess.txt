RewriteEngine On

# Redirige /appointments → appointments.php
RewriteRule ^appointments$ appointments.php [L]

# Opcional: permitir ID en query
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ $1.php [L]